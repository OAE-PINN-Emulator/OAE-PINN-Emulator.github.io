# Reproduce (minimal checklist)

This is a starter reproducibility guide. I’ll make it fully exact once the thesis pipeline is locked.

---

## 1) Get the code

```bash
git clone git@github.com:OAE-PINN-Emulator/OAE-PINN-Emulator.git
cd OAE-PINN-Emulator

